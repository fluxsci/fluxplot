[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "fluxplot"
version = "0.1.0"
description = "matplotlib, but every meaningful thing has a name — semantic SVG plots for the Flux ecosystem"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "Kort Driessen" }]
keywords = ["matplotlib", "svg", "semantic", "scientific-visualization", "flux"]
dependencies = [
  "matplotlib>=3.7,<4",
  "numpy",
  "lxml>=4.9",
  "jsonschema>=4",
]

[project.optional-dependencies]
dev = ["pytest>=7", "cmasher"]
# Perceptually-uniform continuous colormaps for fluxplot.style (cmr.* defaults). The
# style module degrades gracefully to Flexoki-flavoured maps if this isn't installed.
style = ["cmasher"]

[tool.hatch.build.targets.wheel]
packages = ["src/fluxplot"]
# JSON schemas under src/fluxplot/schemas/ ship as package data (default inclusion).

[tool.pytest.ini_options]
testpaths = ["tests"]
